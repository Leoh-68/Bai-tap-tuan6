import 'package:flutter/material.dart';

class FriendsRq extends StatelessWidget {
  const FriendsRq({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Column(
          children: const [Text("FriendRQ")],
        ),
      ),
    );
  }
}
